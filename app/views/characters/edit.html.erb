<% content_for :active_game_id, @game.id %>

<%= render Shared::MobileFrameComponent.new do |frame| %>
  <% frame.with_header do %>
    <%= render Shared::PageHeaderComponent.new(title: @character_form.heading, leading: :back, back_href: @character_form.back_href) %>
  <% end %>

  <%= render @character_form %>
<% end %>
