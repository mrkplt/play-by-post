<% content_for :active_game_id, @game.id %>

<%= render Shared::MobileFrameComponent.new do |frame| %>
  <% frame.with_header do %>
    <%= render Shared::PageHeaderComponent.new(title: @scene_form.heading, leading: :back, back_href: @scene_form.cancel_href) %>
  <% end %>

  <%= render @scene_form %>
<% end %>
