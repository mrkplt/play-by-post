<% if summaries_empty? %>
  <p class="text-muted-2 text-sm">No summaries yet. Summaries appear here once scenes are resolved.</p>
<% else %>
  <% entries.each do |summary| %>
    <%= render Shared::SceneSummaryEntryComponent.new(summary: summary) %>
  <% end %>
  <%== pagy_nav %>
<% end %>
