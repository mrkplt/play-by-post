<div id="<%= dom_id(@post) %>" class="<%= @post.is_ooc? ? 'bg-slate-50 border-l-[3px] border-slate-400 pl-3 opacity-60 hover:opacity-85 text-[0.9em]' : 'border-b border-slate-100' %> py-4 last:border-b-0" data-ooc="<%= @post.is_ooc? %>"<% if @post.is_ooc? %> data-testid="ooc-post"<% end %>>
  <div class="text-xs text-slate-500 mb-1">
    <strong><%= @post.author_display_name %></strong>
    &middot;
    <time data-local-time="<%= @post.created_at.iso8601 %>"><%= @post.formatted_created_at %></time>
    <% if @post.is_ooc? %>
      <span class="inline-block px-2 py-0.5 rounded-full text-xs font-semibold bg-slate-100 text-slate-600">OOC</span>
    <% end %>
    <% if @post.last_edited_at.present? %>
      <span class="italic">(edited)</span>
    <% end %>
    <% if @post.editable_by?(@current_user) %>
      &middot; <%= link_to "Edit", edit_game_scene_post_path(@game, @post.scene, @post), class: "text-[0.8rem]" %>
    <% end %>
  </div>
  <div class="markdown-base post-content" data-testid="post-content"><%= @post.rendered_content.html_safe %></div>
  <% if @post.image.attached? %>
    <div class="mt-2">
      <%= image_tag @post.display_image, class: "max-w-full rounded" %>
    </div>
  <% end %>
</div>
