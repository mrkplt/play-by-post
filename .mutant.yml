---
usage: opensource

environment_variables:
  RAILS_ENV: test
  MUTANT: "1"

hooks:
  - .mutant/hooks.rb

requires:
  - ./config/environment

integration:
  name: rspec

matcher:
  subjects:
    # Models
    - Game
    - GameExportRequest
    - GameMember
    - GameFile
    - Character
    - CharacterVersion
    - Scene
    - SceneParticipant
    - Post
    - User
    - UserProfile
    - Invitation
    - NotificationPreference
    - PostRead
    # Presenters
    - GameFilePresenter
    - PostPresenter
    - ScenePresenter
    - UserPresenter
    # Components
    - Shared::GalleryComponent
    - Shared::NavShellComponent
    - Shared::PostComposerComponent
    - Shared::PostItemComponent
    - Shared::SceneCardComponent
    - Shared::SidebarComponent
    - Shared::TreeNodeComponent
    - Ui::BadgeComponent
    - Ui::BreadcrumbComponent
    - Ui::ButtonComponent
    # Services
    - EmailContentExtractor
    - GameExportService
    # Controllers
    - ActionMailbox::Ingresses::Resend::InboundEmailsController
    - GameExportsController
    - ScenesController
    - CharactersController
    - GamesController
    - SceneParticipantsController
    - PostsController
    - GameFilesController
    - InvitationsController
    - GameMembersController
    - CharacterVersionsController
    - ProfilesController
    - PlayerManagementController
    - Users::SessionsController
    # Jobs
    - PostDigestJob
    - ExportJob
    # Mailers
    - NotificationMailer
    - InvitationMailer
    - ExportMailer
    # Mailboxes
    - SceneMailbox
    # Helpers
    - ApplicationHelper

mutation:
  timeout: 10.0

coverage_criteria:
  timeout: false
  process_abort: false
  test_result: true

# Classes intentionally excluded from mutation testing.
# Each entry must include a comment explaining why it is excluded.
# Prefer adding to matcher.subjects above over adding here.
mutation_exceptions:
  - Current         # ActiveSupport::CurrentAttributes — only attribute declarations, no logic
  - MarkdownRenderer  # no spec yet; add spec then move to matcher.subjects
